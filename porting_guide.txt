1. vector.S
   1) IMAGE_ADDR, should be within IRAM range
   2) IMAGE_SIZE, should be < (IRAM_end - IMAGE_ADDR)

2. linker_script - IRAM(rwx)
   1) ORIGIN - should be equal to IMAGE_ADDR
   2) LENGTH - should be < (IRAM_end - IMAGE_ADDR)

3. soc_memory_map.h - copy from OBDS or SDK

4. iomux_register.h - copy from OBDS or SDK

5. main.c - per application
